{
  "name": "deepharness",
  "version": "0.1.0",
  "private": true,
  "description": "One agent environment, many minds. A local agent runtime that routes each part of the work to the most suitable model.",
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "tsc -b",
    "test": "bun test",
    "typecheck": "tsc -b --dry"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "smol-toml": "^1.3.1",
    "yaml": "^2.6.0",
    "zod": "^3.24.1"
  }
}
